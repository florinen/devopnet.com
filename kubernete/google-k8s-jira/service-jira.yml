apiVersion: v1
kind: Service
metadata:
  labels:
    app: jira
  name: jira
  namespace: apps
spec:
  externalIPs: 
    - 10.10.40.50
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: jira
  type: LoadBalancer
